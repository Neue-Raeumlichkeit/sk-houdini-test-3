/// <references types="houdini-svelte">

/** @type {import('houdini').ConfigFile} */
const config = {
    "watchSchema": {
        "url": "https://staging-gardeners.kinsta.cloud/graphql"
    },
    "plugins": {
        "houdini-svelte": {}
    }
}

export default config
